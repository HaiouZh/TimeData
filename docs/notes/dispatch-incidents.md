# 派单事故日志（dispatch 七查回流入口）

> 每行一条：日期 单号/场景 现象 → 缺口。攒到主题收尾时把仍成立的提进 dev-conventions `_meta/adoption-log.md` 回流候选。
> 本文件 2026-08-21 补建——backlog 2026-08-20 条记录的「落点不存在」自此闭合。**2026-08-20 那条曾临时记进安装版 `~/.claude/skills/dispatch/_meta/adoption-log.md`，该目录已随 skill 升级消失、原文丢失**（正是当时预判的风险）；下面第一条按 backlog 留的摘要重建，细节已不可考。教训：安装版 runtime 不是落点，写进来才算。

- 2026-08-20 （按 backlog 摘要重建，原文已丢）通道中断发生在收尾前、核心产物已合格 → 缺口：判定表没有对应档位——「产物合格但通道断在收尾」既不是 ACCEPTED（结果单没写完）也不是该重派（活已经做对了），当时只能临场判。后续 2026-08-23 tracks-t5-readonly 是同形状的又一例，处置方式（不重派、缩范围续派）可作该档的参考解。
- 2026-08-21 x4a 收割复跑 · 主控复跑 `pnpm --filter client test` 挂死 36min（node 进程 CPU 仅 22s、vitest 无 TTY 偶发悬挂），taskkill 重跑即过 → 缺口：收割复跑长命令该带看门狗（挂 Monitor 观察日志增长，N 分钟零增长即杀重跑），skill 收割节可补一句。
- 2026-08-21 x4a 派单书 · 验收写 `biome check <files>`（含 format+organize-imports 域）而仓库门禁是 `biome lint .`（仅 lint 域），执行器被逼 `--write` 全文件重排出 163 行格式化衍生污染 diff → 缺口：聚焦 lint 的验收命令必须抄仓库 lint script 的真实口径，不许默认 `check`；「本项目落点」的门禁命令字段应含 lint 的准确调用形。
- 2026-08-21 y3-wire 首轮 · 执行器会话被外部中断（events 尾部 "Tool execution aborted"/interrupted:true）而 wrapper 退 0，结果单停在骨架、工作树半成品 184 行 → 假成功「65 形态」又一例：EXITCODE=0 + STATUS 停预填 BLOCKED + COVERAGE 全占位即判骨架；处置 = git restore 半成品 + 重派同单（events 轮转）。缺口：launch.sh 的 65 判据只查 RESULT.md 存在性，可考虑加「STATUS 仍为预填 BLOCKED 且必填字段含『待』」的骨架探测。
- 2026-08-22 kbd 三单批 第一轮三单同死于最终写入被上游掐断（末步 0 token、EXIT=0、REPORT 全丢）→ 缺口①：launcher 的 result_is_skeleton 过滤词只认「待填|TBD|<」，执行器半填 "pending/in progress" 即绕过续跑闸；缺口②：派单书模板未强制「逐命题增量落盘」，产出攒到最后一次写等于押全部产出于最脆的一步（第二轮加了该条款后同样被掐仍保住 P9/P10 全量）。
- 2026-08-22 kbd 三单批 同树并发三张「只读+写自己 .dispatch 目录」单，其一自述目击 4 个白名单外文件瞬时变 M 后自愈（blockerCandidates/TaskWaitingRow 两对）→ opencode 会话快照按工作树共享的已知互踩形态在纯分析单上也会现身；收割时 git status 已净、无实损，但「只读单同树并行安全」的边界应收窄为「连 .dispatch 都不写才算只读」。
- 2026-08-22 kbd 三单批 追记更正：上一条「快照互踩」归因错误。真相是同仓另有一个交互会话（timedata-e5）在并行改 blockerCandidates/TaskWaitingRow 四文件；ios-flash 执行器把它们当「非本任务的脏文件」跑了还原（SPEC_ISSUES 里自述「已还原至基线」——这是违规 git 写操作，收割六查当时只record未深究），调度方后来又 checkout 还原了一次，两次都误伤并行会话的在飞改动（该会话的 harness 会自动重写，实损有限；21:35 状态已存 .dispatch/ghost-blocker-changes-20260822.patch）。缺口③：派单前该查一眼本机有无同仓并行交互会话（ListAgents），有则派单书勿碰清单必须点名它们正在改的文件；缺口④：执行器自述里出现「已还原/已清理」字样应在六查按违规 git 写操作追查，而不是当背景噪音。
- 2026-08-22 tracks-t1-menu 派单书把「`test-buckets.fast-jsdom.json` 按字典序排列」当既定事实写进去，实测原数组本身就不是严格字典序（85 条里存在逆序对）。执行器据此把新条目插在 Sheet 与 Switch 之间（`OverflowMenu` < `Sheet`，局部亦不合序），却在结果单自述「保持字典序」——**基于错前提的判断，连它的自我报告一起是错的**。无功能影响（该文件是 allowlist，消费方只做包含判断，测试确认在快桶跑绿），六查其余全过，判 ACCEPTED。→ 缺口：「按 X 排序/组织」这类**对既有文件结构的描述**与「文件有多少行」同属未验证断言，写单前必须跑一条命令验（`JSON.stringify(a)===JSON.stringify([...a].sort())` 五秒钟），或标「待验」。skill §2 现有的例子都是数量类（「约 900 行」），**结构类没有例子，容易漏**。
- 2026-08-22 tracks-t4b-toolbar 派单书要求 React 组件「函数体开头加 `if (!expanded) return null`」，而 hooks 规则要求所有 hook 无条件调用在前——照字面写会触发 biome `useHookAtTopLevel`。执行器把三个 `useState` 前置、早返回置于其后，语义等价，并如实报进 `SPEC_ISSUES`（同批还报了一条 `import {type T}` 应写 `import type`）。→ 缺口：给 React 组件写「早返回 / 条件渲染」类规格时必须写明**位置约束**（「在所有 hooks 之后」），只说「函数体开头」是与框架规则直接冲突的指令；这类冲突执行器只能二选一，选错就是一次返工。**同时确认一条正向经验**：`SPEC_ISSUES` 字段真的在起作用——两条都是调度方的规格瑕疵，执行器诊断对了、如实报了、还按原规格把活做完了（`SCOPE: FULL`），正是该字段设计时想要的形态。
- 2026-08-23 tracks-t5-readonly 七步单跑到第 3 步被外部中断（events 尾部 `aborted`/`interrupted`、wrapper 退 0），结果单 `STATUS: BLOCKED` 但 **`SCOPE` 仍写 `FULL`**（与 COVERAGE 里五条「待执行」直接矛盾），且 **`FILES` 只列了 1 个文件、漏了已改完 24 行的 `CurrentFrameCard.tsx`**——靠 `git status` 才发现。逐条比对后确认第 3 步其实**完整做完了**（8 个子项全中），只是没来得及回填结果单。处置：**不重派**（会把已正确完成的 1-3 步重写、且与已记录的反证数字对不上账），按 failure-modes §11 缩范围续派第 4-7 步，并在续派书里把这 2 个在飞文件点名为「已完成、不许动」。→ 缺口①：硬条款 1 要求「每完成一步立刻回填」，但**中断发生在两次回填之间时 `SCOPE` 会滞留在乐观值**——`SCOPE` 应改为「最后一步才从 PARTIAL 改成 FULL」，与 `STATUS` 同款处理；缺口②：`FILES` 同样该在**每次落盘产物后立即追加**，而不是收尾统一写，否则中断即漏报。**正向**：六查第 4 条（FILES 与 `git status` 双向比对）这次是唯一发现漏报的通道，其余五查全绿。
- 2026-08-23 tracks-t8-focus 执行器在 `SPEC_ISSUES` 里报：`check:test` 除了本单新建的闸文件外，还报了 **2 条与本单无关的 `dirty-in-clean-bucket`**（`OverflowMenu.test.tsx`、`MilestoneBar.test.tsx`）——那是本批 Task 1 与 Task 4a 新建的 jsdom 测试文件，**建的时候就该登记，两次收割都漏了**。根因在收割方：六查第 2 条「独立复跑验收命令」复跑的是**派单书里写的那几条**，而我给那两单写的验收清单里压根没有 `check:test`，于是「复跑全绿」与「门禁其实是红的」并存了两轮。**判据升级：派单书的验收清单里只要出现「新建测试文件」，就必须带一条 `pnpm check:test`**（本仓的 `dirty-file-in-clean-bucket` 棘轮对每个新 jsdom 测试文件都要求单条登记，这是必然触发、不是偶发）。**正向**：`SPEC_ISSUES` 又一次接住了调度方的规格缺口——执行器诊断对了、按允许的 `--add` 单条补齐（没碰 `--rewrite-baseline`）、如实报告，并把活按原规格做完（`SCOPE: FULL`）。另记一条可复用的闸设计手法：这道结构闸自带一条**自证用例**（先断言扫描范围覆盖得到 `workbench/` 子目录，再断言无违规），因为「递归没生效」与「真的没有违规」在闸的输出上完全同形——**凡是靠遍历目录得出「零违规」的闸，都要先证明它扫到了东西**，否则分母为 0 的绿灯与真绿灯长得一模一样。
- 2026-08-23 tracks-t10-newtrack **新形态：反向放水（执行器改产品代码去迁就错测试）**。派单书里我逐字给的守卫用例用 `input.dispatchEvent(new FocusEvent("blur", {bubbles:true}))` 触发 React 的 `onBlur`——**这是错的**：React 把 `onBlur` 委托到原生 `focusout`（`blur` 不冒泡、委托不到），派 `blur` 在 React 19 下什么都不会发生，那条用例既红不了也绿不了。执行器诊断对了（`SPEC_ISSUES` 写明「React19/jsdom 不触发（需 focusout），已加 blur->focusout 桥接兼容」），但**修法方向反了**：它没改测试，而是在 `NewTrackComposer.tsx` 里加了一段 `useEffect` 监听原生 `blur` 再手动补派 `focusout`——真实浏览器里 `blur` 之后本来就跟着 `focusout`，这段只会让 `onBlur` 触发两次，是纯粹为了让错测试变绿而存在的产品代码。收割方改测试用 `focusout`、删掉桥接，两条变异（删 `onBlur` → 「空输入收回」红；`if (!title.trim())` 改无条件 → 「有内容保持展开」红）各打中一半，恰好 1 红 3 绿。→ 缺口：**六查第 5 条（测试放水）只盯 `git diff -- '*test*'`，抓的是「改测试让它变绿」，抓不到反向的「改实现让错测试变绿」**——后者的痕迹全在实现文件里。**新判据：`SPEC_ISSUES` 或 `DECISIONS` 里出现「兼容 / 桥接 / 适配 / workaround」这类词时，去实现 diff 里找那段代码，问一句「它在真实运行环境（不是测试环境）里是不是死代码」**；是的话就是方向反了，该修的是测试。**通则**：派单书里逐字给出的测试代码，其中的**事件名、API 语义**和「基线多少条」一样属于未验证断言——`FocusEvent("blur")` 看起来天经地义，实际错了，而且错得很安静（不报错、不失败，只是什么都不做）。
- 2026-09-02 fr-d2（终审「状态与时序」镜头）**跑成了却零产出**：事件流末尾 `"reason":"length"`——执行器把七条具名问题的推演全写在回复正文里，撞到输出长度上限被截断，磁盘上只剩预填 `BLOCKED` 的骨架、FINDINGS.md 根本没建；退出码 0，栅栏汇总里与「正常完成」只差 STATUS 一列。硬条款 1「边做边落盘」对**长回答型只读单**不够具体——它说的「每完成一步」执行器理解成「整单完成再写」。重派 d2b 时加了三条：开工先建文件、每答完一个具名问题立刻追加、每条 ≤12 行且正文里不重复——一次跑成。→ 缺口：**镜头类派单书的输出节要写「按问题编号逐条追加进文件，正文只回『已写入』」**，而不是只在硬条款里泛泛说落盘；正向：栅栏的 STATUS 列 + 查 0「结果单还停在骨架」这一问当场把它从六条 DONE 里挑了出来，没被当成「未发现问题」。另记一条主控自伤：修复波提交前用 `git checkout -- <file>` 还原变异，把**未提交的修复一并还原了**——变异反证必须在提交之后做，或从副本还原。
- 2026-09-16 ios-p1-t1 两条缺口，都不影响本单判定（ACCEPTED）。①**收割自检器与派单模板互相打架**：模板要求 `FILES` 每落盘一次就追加一行，执行器照做写成 5 行 `FILES:`，而 `check-harvest.mjs` 的查 4 只解析第一行 `FILES:`，于是把另外三个**已如实申报**的文件报成「改了没说（漏报）」——那正是查 4 宣称唯一能抓的那类缺陷，误报一次就等于把这条通道的信噪比打穿。人工比对结果单全文才排除。修法在脚本侧：`FILES` 应合并全部 `FILES:` 行再比对（或模板改成单行块，但那会退回 §5 已修过的「中断即漏报」）。②**我自己违反了「预期必须先验」那条**：派单书写「预期是编译/导入失败」，实测是行为失败（TS 里缺失的导出在运行时是 `undefined`，不会中断导入，8 条红在断言而非导入）。执行器诊断对了、如实报进 `SPEC_ISSUES`、照原规格做完。skill §1 已有这条规矩，缺的不是规矩是执行——**「跑之前会怎么红」这类话，要么自己先跑一次，要么逐条标『待验』**，零成本。
- 2026-09-16 ios-p1-t3b **按行号区间从 plan 里截规格，静默截掉了整整一个 Step**。本批的派单书是脚本拼的：`<自写 HEAD> + plan[起始行..结束行] + 公共尾块`。t3b 的区间写成 1263–1356，而 Step 13（`isStorageOpen` 的逐字实现）从 1357 起——**派单书里压根没有那段代码，脚本不报错、`check-task` 也不报**（它检的是路径可达性，不是「区间有没有切在小节边界上」）。执行器按我在 HEAD 里列的接口签名把它推了出来，函数体与 plan 逐字相同、只有文档注释不一样，**判定没受影响纯属运气**：签名一行就把实现钉死了，换个复杂点的 Step 就是静默缩水。→ 缺口：**区间截取必须验「结束行的下一行不是本任务的另一个 Step」**，或者干脆改成按小节标题切（找下一个 `^### Task` / `^- \[ \] \*\*Step N\+1`）。附带一条正向：执行器把这件事如实报进了 `SPEC_ISSUES`（「规格缺 isStorageOpen 逐字实现，按接口推断」），**这是本批第二次靠 `SPEC_ISSUES` 接住调度方的规格缺口**；另一条 `SPEC_ISSUES` 是它指出我背景里写「storageProbe.ts 全文 11 行」而实测 12 行——数量类既定事实又一次没验就写。
- 2026-09-16 ios-p1-t6 **plan 逐字给的守卫用例里有一条假闸，执行器自己查出来并如实报了**。变异表 M3 要求「`probeDone` 只看 `ttiMs`、不认 stall 结论」，期望 `stall 有结论也算探针结束：reload 收尾带上 stall` 变红——实测**全 11 条仍绿**。原因：那条用例走的是 `end("reload")`，而 `end()` 无条件收尾、根本不经过 `tryComplete` 的 `probeDone` 判断，**用例名承诺的那个分支它一次都没走到**。执行器没改测试去凑，`SPEC_ISSUES` 写清了为什么。收割侧按既定做法处置：**原例保留**（它守的是「reload 收尾带得上 stall」，那条仍成立）、**补一条真闸**（探针永不落地、只有 stall 结论，另两件事齐 → 应判 `complete` 而不是拖到 20 s 上限）、**做反证**（删掉 `|| session.stall !== null` 后新闸红 1 条、还原复绿）。→ 缺口不在派单侧而在**写 plan 侧**：变异表是我照着 plan 的「Step 6 自证变异」那行抄的，而那一行只写了变异动作、没写期望变红的用例——**是我在派单书里替它配的用例名，配错了**。判据升级：**给变异配用例名时，要能说出「这条用例的哪一次调用会走到被守护的那行」**，说不出就是在猜。
- 2026-09-16 ios-p1-t7 **又一条假闸，但成因与 t6 那条完全不同：变异被同一模块里的第二道守卫吸收了**。变异表 M3 要求「`dispose` 只清宽限那一枚定时器、不清直驱那一枚」，期望 `补拍之后卸载 → 直驱与宽限两枚定时器都跟着走` 变红——实测**全 30 条仍绿**。原因：`ArmedProbe.schedule` 在 `setTimeout` 回调里还有一道 `if (!probe.disposed) task()`，漏清的定时器到点**照样什么都不做**，于是那条用例断言的三个可观测量（`driveSchedulerDirectly` / `onDeadlock` / `stashPendingReport`）一个都不会变——**它测的是「有没有做事」，而变异改的是「定时器还挂不挂着」**。执行器没改测试去凑，`SPEC_ISSUES` 写清了「schedule 内 disposed 二次守卫兜住未清定时器，属假闸」。收割侧按 t6 的既定做法处置：**原例保留**（它守的是「卸载后不再做事」，那条仍成立）、**补一条真闸**（补拍后卸载 → `vi.getTimerCount()` 必须从 3 归 0）、**做反证**（`dispose` 改成只清最后一枚 → 新闸红 1 条、报 `expected 1 to be +0`，原例仍绿，还原后 31 全绿）。→ 缺口：**给带纵深防御的模块配变异时，要先问「这一层被改坏之后，另一层会不会把症状吃掉」**——`disposed` 标志位 + `clearTimeout` 是同一件事的两道闸，删任一道，行为层的断言都看不出来；闸要盯**只有被变异那一层能改变的可观测量**（这里是待定定时器条数，不是行为）。这与 t6 那条（用例压根没走到被守护的分支）是两种不同的失效，**判据要各记一条**。另记一条同批的规格瑕疵：plan 逐字给的 `vi.mock("…/pendingReports.ts", () => ({ stashPendingReport }))` 这种**裸工厂 mock**，在上一单给 `schedulerProbeReport.ts` 加了 `import { SYNC_LOG_DETAIL_MAX } from "./pendingReports.ts"` 之后会让 19 条用例炸在「访问缺失导出」上；执行器照同文件里既有的 `importOriginal` 写法补救、断言意图未动、如实报进 `SPEC_ISSUES`。**判据：写 plan 时凡是给某个模块写裸工厂 mock，都要查一遍本批有没有别的任务往那个模块加导出**——加了就得用 `importOriginal` 摊平，否则前后两单必然对撞。
