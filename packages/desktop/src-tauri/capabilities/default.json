{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "两个窗口的基础能力：core 事件（listen 用）与自定义命令",
  "windows": ["main", "capture"],
  "permissions": ["core:default"]
}
