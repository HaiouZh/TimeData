{
  "name": "@timedata/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "bin": {
    "timedata": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "pnpm --filter @timedata/shared build && tsc --noEmit"
  },
  "dependencies": {
    "@timedata/shared": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.0"
  }
}
