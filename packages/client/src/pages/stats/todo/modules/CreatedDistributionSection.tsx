import type { TodoStatsModuleProps } from "../types.ts";

export default function CreatedDistributionSection(_props: TodoStatsModuleProps) {
  return <section className="rounded-card border border-border bg-surface p-4 shadow-elev1">创建分布</section>;
}
